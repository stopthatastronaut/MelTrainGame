# unzip the files
Expand-Archive -Path ./../../site/data/lines.json.zip `
      -DestinationPath ./../../site/data -verbose -force
Expand-Archive -Path ./../../site/data/stops.json.zip `
      -DestinationPath ./../../site/data -verbose -force

# load them

# filter them down

